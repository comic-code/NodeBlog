<%- include('partials/header.ejs') %>
<%- include('partials/homenavbar.ejs') %>
<body>
  <div class="container py-3">  

    <% result.articles.rows.forEach(article => { %>
      <a href="/<%= article.slug %>">
        <div class="card my-3 customCard">
          <div class="card-header">
            <h2><%= article.title %></h2>
          </div>
          <div class="card-body">
            
          </div>
        </div>
      </a>
    <% }) %>

    <div class="row">
      <div class="d-flex justify-content-center">

        <% if(result.page > 2) { %>
          <a href="/articles/page/<%= result.page - 1%>" class="btn customButton">
            < Anterior
          </a>
        <% } else if(result.page == 2) { %>
          <a href="/%>" class="btn customButton me-5">
            < Anterior
          </a>
        <% } %>

        <% if(result.next) { %>
          <a href="/articles/page/<%= result.page + 1%>" class="btn customButton">
            Próximo >
          </a>
        <% } %>

      </div>
    </div>
    
  </div>
</body>
<%- include('partials/footer.ejs') %>
